// Shared badge colour palette — used by the beat-marker badges (useBeatMarkers)
// and the thread header chip (CommentThread) so the same thread number always
// renders the same colour in both places.
export const BADGE_PALETTE = ['#1fb6a6', '#7c5cff', '#e0a000', '#e0608a', '#4c8dff', '#5fae3c']
